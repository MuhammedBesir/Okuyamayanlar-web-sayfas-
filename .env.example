# ===========================================
# 🔐 ENVIRONMENT VARIABLES GUIDE
# ===========================================
# Bu dosyayı kopyalayıp .env.local oluşturun (local development için)
# Production'da Vercel Dashboard'dan ayarlayın

# ===========================================
# 📊 DATABASE (ZORUNLU)
# ===========================================
# Local Development:
DATABASE_URL="postgresql://USER:PASSWORD@localhost:5432/okuyamayanlar?schema=public"

# Vercel Production (Vercel Postgres kullanıyorsanız):
# Vercel Dashboard'da "Storage" -> "Postgres" oluşturun
# Custom Prefix olarak "POSTGRES" seçin
# Sonra Environment Variables'a şunu ekleyin:
# DATABASE_URL="${POSTGRES_PRISMA_URL}"
# Vercel otomatik olarak ${POSTGRES_PRISMA_URL} değişkenini inject edecek

# Manuel PostgreSQL kullanıyorsanız:
# DATABASE_URL="postgresql://user:password@host:5432/database?sslmode=require"

# ===========================================
# 🔑 NEXTAUTH (ZORUNLU)
# ===========================================
# Secret key - random string oluşturun:
# PowerShell'de: [Convert]::ToBase64String((1..32 | ForEach-Object { Get-Random -Minimum 0 -Maximum 256 }))
NEXTAUTH_SECRET="your-random-secret-key-here-change-this-in-production"

# Production URL - Vercel'de otomatik set edilir, local'de:
NEXTAUTH_URL="http://localhost:3000"

# Production'da Vercel şöyle set eder:
# NEXTAUTH_URL="https://your-domain.vercel.app"

# ===========================================
# 🔐 GOOGLE OAUTH (OPSİYONEL - Google ile giriş için)
# ===========================================
# Google Cloud Console'dan OAuth 2.0 Client ID alın:
# 1. https://console.cloud.google.com
# 2. "APIs & Services" -> "Credentials"
# 3. "Create Credentials" -> "OAuth 2.0 Client ID"
# 4. Application type: "Web application"
# 5. Authorized redirect URIs:
#    Local: http://localhost:3000/api/auth/callback/google
#    Prod: https://your-domain.vercel.app/api/auth/callback/google
# 6. Copy Client ID ve Client Secret

GOOGLE_CLIENT_ID="your-google-client-id"
GOOGLE_CLIENT_SECRET="your-google-client-secret"

# ===========================================
# 📧 EMAIL (ŞİFRE SIFIRLAMA İÇİN - ZORUNLU)
# ===========================================
# Gmail kullanıyorsanız:
# 1. Google Account -> Security -> 2-Step Verification (aktifleştirin)
# 2. App Passwords -> "Mail" seçip şifre oluşturun
# 3. Oluşan 16 haneli şifreyi EMAIL_PASSWORD'e yazın

EMAIL_HOST="smtp.gmail.com"
EMAIL_PORT="587"
EMAIL_USER="your-email@gmail.com"
EMAIL_PASSWORD="your-app-specific-password"
EMAIL_FROM="Okuyamayanlar <noreply@okuyamayanlar.com>"

# Outlook/Hotmail kullanıyorsanız:
# EMAIL_HOST="smtp-mail.outlook.com"
# EMAIL_PORT="587"
# EMAIL_USER="your-email@outlook.com"
# EMAIL_PASSWORD="your-password"
# EMAIL_FROM="Okuyamayanlar <noreply@okuyamayanlar.com>"

# ===========================================
# 🗺️ GOOGLE MAPS (ETKİNLİKLER İÇİN - OPSİYONEL)
# ===========================================
# Google Cloud Console'dan API key alın:
# 1. https://console.cloud.google.com
# 2. "APIs & Services" -> "Credentials"
# 3. "Create Credentials" -> "API Key"
# 4. "Maps JavaScript API" ve "Places API" aktifleştirin
# 5. API key'i buraya yapıştırın

NEXT_PUBLIC_GOOGLE_MAPS_API_KEY="your-google-maps-api-key"

# ===========================================
# 🎨 NEXT.JS PUBLIC VARIABLES (OPSİYONEL)
# ===========================================
# Public değişkenler - frontend'de erişilebilir
# NEXT_PUBLIC_ prefix'i zorunlu!

NEXT_PUBLIC_APP_NAME="Okuyamayanlar"
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# Production'da:
# NEXT_PUBLIC_APP_URL="https://your-domain.vercel.app"

# ===========================================
# 📝 NOTLAR
# ===========================================
# ✅ ZORUNLU değişkenler: DATABASE_URL, NEXTAUTH_SECRET, NEXTAUTH_URL, EMAIL_*
# ⚠️  Production'da MUTLAKA değiştirin: NEXTAUTH_SECRET, EMAIL_PASSWORD
# 🔒 .env.local'i .gitignore'a ekleyin (zaten eklendi)
# 🚀 Vercel'de Environment Variables'ı "Settings" -> "Environment Variables" kısmından ekleyin
# 🔄 Environment variable değişikliği sonrası yeniden deploy edin
